apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "op-batcher.fullname" . }}
  labels:
    {{- include "op-batcher.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "op-batcher.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- toYaml .Values.podAnnotations | nindent 8 }}
      labels:
        {{- include "op-batcher.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "op-batcher.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
        - op-batcher
        args:
        - --l1-eth-rpc={{ .Values.config.l1.rpcUrl }}
        - --l2-eth-rpc={{ .Values.config.l2.rpcUrl }}
        - --rollup-rpc={{ .Values.config.rollup.rpcUrl }}
        - --max-channel-duration={{ .Values.config.maxChannelDuration | default 1 }}
        - --sub-safety-margin={{ .Values.config.subSafetyMargin | default 4 }}
        - --poll-interval={{ .Values.config.pollInterval | default "6s" }}
        - --num-confirmations={{ .Values.config.numConfirmations | default 1 }}
        - --safe-abort-nonce-too-low-count={{ .Values.config.safeAbortNonceTooLowCount | default 3 }}
        {{- if eq .Values.config.dataAvailability.type "blobs" }}
        # Use EIP-4844 blobs for data availability (cheaper post-Dencun)
        - --data-availability-type=blobs
        {{- if .Values.config.l1.beacon.url }}
        - --l1-beacon={{ .Values.config.l1.beacon.url }}
        {{- end }}
        {{- else if eq .Values.config.dataAvailability.type "calldata" }}
        # Legacy calldata mode (more expensive)
        - --data-availability-type=calldata
        - --batch-type={{ .Values.config.batchType | default 1 }}
        {{- end }}
        {{- if .Values.config.privateKey }}
        - --private-key={{ .Values.config.privateKey }}
        {{- else }}
        - --private-key=/secrets/private-key
        {{- end }}
        {{- if .Values.config.sequencerHd }}
        - --sequencer-hd={{ .Values.config.sequencerHd }}
        {{- end }}
        {{- if .Values.config.metrics.enabled }}
        - --metrics.enabled
        - --metrics.addr={{ .Values.config.metrics.addr }}
        - --metrics.port={{ .Values.config.metrics.port }}
        {{- end }}
        {{- if .Values.config.pprof.enabled }}
        - --pprof.enabled
        - --pprof.addr=0.0.0.0
        - --pprof.port=6060
        {{- end }}
        ports:
        - name: metrics
          containerPort: {{ .Values.config.metrics.port }}
          protocol: TCP
        {{- if .Values.config.pprof.enabled }}
        - name: pprof
          containerPort: 6060
          protocol: TCP
        {{- end }}
        livenessProbe:
          httpGet:
            path: /metrics
            port: metrics
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /metrics
            port: metrics
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
        volumeMounts:
        - name: secrets
          mountPath: /secrets
          readOnly: true
      volumes:
      - name: secrets
        secret:
          secretName: {{ include "op-batcher.fullname" . }}-secrets
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

